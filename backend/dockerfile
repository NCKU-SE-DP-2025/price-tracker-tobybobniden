# 使用官方 Python 映像作為基礎
FROM python:3.10-slim

# 設定容器內的工作目錄
WORKDIR /app

# 複製 requirements.txt 並安裝依賴
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 複製整個 backend 目錄進容器
COPY backend/ .

# 確保 Python 可以找到 src 模組
ENV PYTHONPATH=/app

# 對外開放 8000 port
EXPOSE 8000

# 啟動 FastAPI 應用
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]